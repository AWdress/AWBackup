#!/bin/bash
# ============================================
# 配置文件示例
# ============================================
# 复制此文件到项目根目录并重命名为 config.conf
# cp examples/config.example.conf config.conf

# 然后根据你的需求修改配置

# ============================================
# 最小配置示例
# ============================================
BACKUP_TASKS="mybackup"
LOG_DIR="$(dirname "$0")/logs"
LOG_RETENTION_DAYS=7
ENABLE_NOTIFICATION=true

MYBACKUP_NAME="我的备份"
MYBACKUP_SOURCE="/mnt/user/data"
MYBACKUP_DESTINATION="/mnt/user/backups"
MYBACKUP_RETENTION_DAYS=7
MYBACKUP_COMPRESS_LEVEL=6
MYBACKUP_ENABLED=true

# ============================================
# Telegram 通知（可选）
# ============================================
ENABLE_TELEGRAM=false
TELEGRAM_BOT_TOKEN=""  # 从 @BotFather 获取
TELEGRAM_CHAT_ID=""    # 从 @userinfobot 获取

